<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
  <title>{{ config.title }}</title>
  <subtitle>{{ config.description }}</subtitle>
  <link href="{{ feed_url | safe }}" rel="self" type="application/atom+xml"/>
  <link href="{{ config.base_url | safe }}"/>
  <generator uri="https://www.getzola.org/">Zola</generator>
  <updated>{{ last_updated | date(format="%+") }}</updated>
  <id>{{ feed_url | safe }}</id>
  {%- for page in pages %}
  <entry xml:lang="{{ lang }}">
    <title>{{ page.title }}</title>
    <published>{{ page.date | date(format="%+") }}</published>
    <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
    <link href="{{ page.permalink | safe }}" rel="alternate" type="text/html" title="{{ page.title }}"/>
    <id>{{ page.permalink | safe }}</id>
    {%- if page.extra.excerpt %}
    <summary>{{ page.extra.excerpt }}</summary>
    {%- elif page.summary %}
    <summary>{{ page.summary }}</summary>
    {%- endif %}
    <content type="html" xml:base="{{ page.permalink | safe }}">{{ page.content }}</content>
  </entry>
  {%- endfor %}
</feed>
